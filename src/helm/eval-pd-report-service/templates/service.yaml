apiVersion: v1
kind: Service
metadata:
  name: {{ include "eval-pd-report-service.fullname" . }}
  labels:
    {{- include "eval-pd-report-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eval-pd-report-service.selectorLabels" . | nindent 4 }}
