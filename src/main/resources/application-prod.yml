# Production Environment Configuration

spring:
  # AWS Secrets Manager Configuration
  # Secrets will be loaded from: /secret/eval-pd-report-service/prod
  cloud:
    aws:
      secretsmanager:
        enabled: true

  # Database Configuration (values loaded from Secrets Manager)
  datasource:
    hikari:
      maximum-pool-size: 50
      minimum-idle: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # Redis Configuration (values loaded from Secrets Manager)
  data:
    redis:
      timeout: 2000
      ssl: true
      lettuce:
        pool:
          max-active: 50
          max-idle: 25
          min-idle: 15

  # Security - OAuth2 Configuration
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${OIDC_ISSUER_URI:https://placeholder-oidc-provider.com}

# AWS Service Configuration
aws:
  s3:
    bucket-name: ${S3_BUCKET_NAME:ev-plus-reports-prod}
  sqs:
    queue-name: ${SQS_QUEUE_NAME:ev-plus-reporting-prod-queue}
  email:
    from-address: ${EMAIL_FROM_ADDRESS:noreply@evplus.com}
    enabled: true

# Logging - Minimal logging in production
logging:
  level:
    com.evplus.report: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
